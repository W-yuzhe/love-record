# 2026-09-05

## Trip 足迹地图改造
- 将足迹地图从「城市小圆点」改为「省份面点击」：点击省份区域即可点亮该省对应城市。
- 新增 `src/data/cityProvinceMap.ts`，建立 34 个城市与 SVG 省份 ID 的映射。
- 重写 `src/components/map/StaticChinaMap.tsx`：
  - 保持 1000:776 宽高比，避免地图被拉伸变形。
  - 移除绝对定位小圆点，改为让 SVG 省份 path 可点击、hover 反馈。
  - 保存记忆后，对应整个省份面变为粉紫色 `#D68FB8`。
  - 点击省份时不触发地图拖拽。
- 更新 `src/pages/MapPage.tsx`：
  - 省份含多个城市时弹出城市选择浮层；单城市直接打开表单。
  - 图例同步改为省份面色块。
- `tsc -b` 通过；完整 `npm run build` 因环境 safe-delete 异常，使用 `--emptyOutDir=false` 可正常构建。

## 地图与回忆录交互增强
- 地图标签：默认显示省份名称；缩放比例 ≥ 2.2 时切换为显示城市名称（基于 SVG path getBBox 计算省份中心）。
- 取消点亮：在 Trip 城市表单中新增「取消点亮」按钮，清空记忆并将省份恢复为未点亮状态。
- 删除回忆：在回忆录列表卡片右上角新增删除按钮，确认后删除本地/Superbase 数据。
- 首页封面：大标题字重从 `font-semibold` 改为 `font-light`，阴影减弱，与「恋爱记录」小字风格统一。

## 地图两级视图与 UI 统一
- 新增 `src/components/map/ProvinceCityMap.tsx`：从全国 SVG 提取指定省份 path，生成省份放大视图。
- 重写 `src/components/map/StaticChinaMap.tsx`：全国视图不显示省份/城市名称，点击省份触发进入省份视图。
- 重写 `src/pages/MapPage.tsx`：
  - `country` / `province` 两级视图切换。
  - 省份视图显示该省份内所有预置城市（城市点 + 城市名），点击城市打开表单。
  - 单城市省份直接打开表单；多城市省份进入省份视图。
  - 保存点亮城市后返回全国视图，对应省份同步变亮。
- 修复 `src/pages/Memories.tsx`：
  - 修复删除按钮导致卡片高度塌陷问题。
  - 添加视频 URL 识别与图片加载失败隐藏占位，修复回忆录显示异常。
- 首页与导航字体统一：恋爱记录小字、From... 文案、+ New Memory、Pick a Star、NavBar +Memory 均改为 `font-serif italic`。
- `tsc -b` 与 `vite build --emptyOutDir=false` 均通过。

## 大改版：舍弃 Trip，回忆录改为相册制
- 移除 `Trip`：从 `NavBar`、`BottomNav`、`App.tsx` 路由中移除 `/map` 入口（相关组件文件保留但不再使用）。
- 新增相册弹层组件 `src/components/memories/PhotoNoteModal.tsx`：左图右文便签布局，含编辑入口与 ESC 关闭。
- 新增 `src/pages/MonthAlbum.tsx`：点击首页月份卡片进入该月份独立相册；自动按月生成（基于 `getMonthsBetween`）。
- 重写 `src/pages/Memories.tsx`：
  - 标题仅保留英文 `Memories`，放大并使用 `font-serif italic`。
  - 改为瀑布流相册网格，点击照片弹出便签弹层。
  - 保留搜索、标签筛选与删除功能。
- 新增 `src/pages/EditMemory.tsx`：基于 CreateMemory 改造，支持编辑标题、日期、地点、天气、心情、标签、描述与媒体。
- 数据层增强：
  - `src/lib/localMemories.ts` 新增 `updateLocalMemory`。
  - `src/api/memories.ts` 新增 `updateMemory` API（含 Supabase 与本地回退）。
  - `src/hooks/useMemories.ts` 暴露 `updateMemory`。
  - `src/types/index.ts` 中 `MemoryMedia.type` 扩展为 `'image' | 'video' | 'audio'`。
- `Home` 与 `Experience` 内容独立：首页月份进入 `/album/:monthKey`，不再带 `?month=` 跳转到 `/memories`。
- `tsc -b` 与 `vite build --emptyOutDir=false` 均通过。

## 回忆录按日期便签化与照片级交互
- 扩展 `MemoryMedia` 类型：新增 `note`（便签）、`likes`（点赞数）、`is_cover`（封面）。
- 新增 `src/lib/localMonthCovers.ts`：为首页 `Our Timeline` 月份卡片提供独立封面存储。
- 数据层增强：
  - `src/lib/localMemories.ts` 新增 `updateLocalMemoryMedia`。
  - `src/api/memories.ts` 新增 `updateMemoryMedia` API。
  - `src/hooks/useMemories.ts` 暴露 `updateMemoryMedia`。
- 重写 `src/pages/MonthAlbum.tsx`：
  - 按日期倒序分组，每个日期一张「便签」卡片。
  - 同一日期多张照片折叠，显示封面，可展开查看全部。
  - 点击照片弹出右侧笔记栏（`PhotoNoteModal`），支持书写便签、保存、删除、点赞、设为封面。
  - 支持将照片设为月份封面（写入 `localMonthCovers`）。
- 重写 `src/pages/Memories.tsx`：
  - 按日期倒序排列，同一日期的照片放在同一个便签卡片内。
  - 日期标签使用年月日样式，最新日期在最上方。
  - 点击照片弹出笔记栏，支持书写、保存、删除、点赞、设为封面。
- 更新 `src/pages/Home.tsx`：月份卡片封面优先读取 `localMonthCovers`，没有再取回忆首图。
- `tsc -b` 与 `vite build --emptyOutDir=false` 均通过。

## UI 微调与创建来源区分
- `NavBar` 中 `days of us` 文字改为浅香槟金色 `text-star-gold`。
- 修复新增按钮文字：Memories / NavBar / MonthAlbum 的按钮统一为图标 + `Memory` / `New Memory` / `New Moment`，并加 `whitespace-nowrap` 避免换行重复显示 `+`。
- 重写 `src/pages/MonthAlbum.tsx`：
  - 照片尺寸整体缩小约 50%（封面约 180×180，缩略图约 80×80）。
  - 每个日期卡片改为左侧封面图、右侧缩略图 + 便签文字区域；保存的 note 显示在图片右侧空白处。
  - 无论是否有照片，顶部始终显示 `New Moment` 新增按钮。
- 区分创建来源：
  - `MonthAlbum` 新增按钮链接到 `/memories/new?from=album&month=...`。
  - 顶部导航/悬浮按钮的 `+ Memory` 链接到 `/memories/new`。
  - `CreateMemory` 根据 `from=album` 显示简化界面（`New Moment`），隐藏地点/天气/心情/标签，保存后返回对应月份相册。
- `tsc -b` 与 `vite build --emptyOutDir=false` 均通过。

## 本地持久化修复与 Experience 右侧笔记栏
- 修复照片/视频刷新后消失：
  - `src/lib/localMemories.ts` 中 `generateLocalMedia` 改为异步，将上传的 File 转为 base64 Data URL 持久化到 `localStorage`。
  - 新增 `src/utils/file.ts`：提供 `fileToBase64`、`fileType` 通用工具。
  - `src/api/memories.ts` 中 `createMemory` / `updateMemory` 的本地与 Supabase 路径均根据 `file.type` 正确识别 `image` / `video` / `audio`。
- 修复视频无法播放：媒体 `type` 不再硬编码为 `image`，视频保存为 `video` 并在展示处用 `<video controls playsInline>` 渲染。
- `NavBar` 与移动端菜单中的 `days of us` 整体（含数字）改为浅香槟金色 `text-star-gold`。
- 重写 `src/pages/Memories.tsx`：
  - 每个日期卡片改为左侧日期标签 + 封面图，右侧缩略图 + 便签文字区域（Experience 照片栏右侧新增笔记栏）。
  - 保持按日期倒序、同一日期折叠、搜索与标签筛选。
- 增强 `src/components/memories/PhotoNoteModal.tsx`：
  - 支持修改日期。
  - 支持增删标签/分类。
  - 支持追加照片（调用 Memories 中的文件选择器）。
  - 保留便签、点赞、设为封面、删除功能。
- `MonthAlbum.tsx` 照片尺寸再缩小 50%（封面约 90×90，缩略图约 40×40）。
- 更新 `DEPLOY.md`：移除已废弃的 `VITE_TENCENT_MAP_KEY` 与腾讯地图白名单步骤。
- `tsc -b` 与 `vite build --emptyOutDir=false` 均通过。

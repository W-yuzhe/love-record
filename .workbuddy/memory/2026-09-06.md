# 2026-09-06

## 2026-09-06 17:40 网易云音乐悬浮播放器
- 新增 `src/components/music/FloatingMusicPlayer.tsx`：右下角圆形音乐按钮，点击展开玻璃面板。
- 使用网易云官方 iframe 外链播放器：`https://music.163.com/outchain/player?type=0&id={歌单ID}`。
- 支持 localStorage 持久化展开/收起状态。
- 无 `VITE_NETEASE_PLAYLIST_ID` 时自动隐藏整个播放器。
- 已更新 `src/App.tsx` 全局挂载播放器；`.env.example` 新增 `VITE_NETEASE_PLAYLIST_ID` 配置说明。
- 注意：浏览器限制 autoplay，用户进入页面后需手动点击 iframe 内播放按钮才能出声。
- `tsc -b` 与 `vite build --emptyOutDir=false` 通过，已提交 `e078c4a`。`git push` 因凭证/网络问题失败，需用户开 VPN 后手动推送。

## 2026-09-06 16:50 Memories（Experience）界面重构
- 日期分组封面改为重叠样式 `CoverStack`：只显示封面大图，后方偏移露出下一张边缘，右上角显示 `+N` 张数徽章。
- 多张图片时只展示封面，剩余照片通过「展开 N 张」按钮显示在分组下方网格中。
- 便签区域扩大并上移：放在年份/统计行下方，使用更大的内边距和字号；无便签时提示「点击封面照片添加便签」。
- 新增可编辑 Experience 背景：
  - `useExperienceBackground` hook：通过 localStorage 持久化背景配置。
  - `ExperienceBackgroundPanel`：支持输入 public 文件夹图片路径（如 `/experience-bg.png`）或本地上传压缩后的 base64。
  - Memories 页面头部新增「背景」按钮，可设置/预览/清除背景；默认保持原深色渐变。
- 文件改动：
  - 新增 `src/components/memories/CoverStack.tsx`
  - 新增 `src/components/memories/ExperienceBackgroundPanel.tsx`
  - 新增 `src/hooks/useExperienceBackground.ts`
  - 重写 `src/pages/Memories.tsx`
- `tsc -b` 与 `vite build --emptyOutDir=false` 均通过。
- 已提交 `e97c2a6`，但 `git push` 仍因网络问题失败，需用户开 VPN 后手动推送。

## Vercel 部署 404 排查
- GitHub 仓库 `https://github.com/W-yuzhe/love-record` 已存在且为 Public，包含最新代码。
- 本地 `main` 分支与 `origin/main` 同步，工作区干净。
- 项目目录下没有 `.vercel` 配置，说明未通过 CLI 部署。
- 用户访问的 `https://love-record-site-xxx.vercel.app/` 中 `xxx` 是占位符，且该部署可能已被删除或项目未在 Vercel 上创建。
- 下一步：在 Vercel Dashboard 重新 Import 仓库并部署，或找回已有项目域名。

## Supabase 连接与 localStorage 配额问题
- 用户已在 Vercel 配置环境变量，网站成功连接到 Supabase，但出现两个错误：
  1. `伴侣关系尚未初始化完成`：说明 `schema.sql` 未执行或 `couples` 表未创建。
  2. `Failed to execute 'setItem' on 'Storage': ... exceeded the quota`：`localStorage` 因 base64 图片/视频过大而满。
- 已更新 `supabase/schema.sql`：
  - `memory_media.type` 的 CHECK 约束扩展为 `('image','video','audio')`。
  - 新增 `note`、`likes`、`is_cover` 字段，与前端数据模型对齐。
  - `profiles.email` 改为可空、移除 UNIQUE，兼容匿名用户空邮箱。
- 已修复 `src/contexts/AuthContext.tsx`：匿名用户没有 email 时，自动生成唯一占位邮箱，避免 `profiles.email` 唯一约束冲突。

## 2026-09-06 16:05 星空背景图引用修复
- 用户反馈 stars 图丢失：发现 `public/star-sky.jpg` 已被替换为 `public/star-sky.png`，但 `src/pages/StarrySky.tsx` 仍引用 `/star-sky.jpg`。
- 已更新 `StarrySky.tsx` 为 `/star-sky.png`，同步更新 DEPLOY.md、USAGE.md。
- 本地构建通过（`npx vite build --emptyOutDir=false`），并提交 `5417f0c`。
- `git push` 因网络 SSL/TLS handshake 失败，需用户开启 VPN 后手动推送：
  ```bash
  git push
  ```

## 2026-09-06 15:30 体验细节优化
- 首页背景图：`public/hero-bg,jpg`（命名有误）转码并规范为 `public/hero-bg.png`，同步更新 `Home.tsx`、DEPLOY.md、USAGE.md 引用。
- 图片弹层便签：默认以普通文本（带引号）只读展示，新增「编辑」按钮切换为 textarea，保存后自动回到只读模式。
- MonthAlbum 弹层补齐「追加照片」按钮，逻辑与 Memories（Experience）一致：通过隐藏 file input 选择图片/视频，压缩后转 base64 并写入当前 memory。
- 已执行 `npm run build` 通过并推送至 GitHub（`c8c175e`）。

## 性能与体验优化
- 新增 `vercel.json`：配置 SPA 路由回退到 `index.html`，解决直接访问子路由刷新 404。
- 压缩 `public/star-sky.jpg`：从 5.8MB 降至约 0.82MB，提升 Stars 页面加载速度。
- 新增 `src/utils/image.ts`：基于 canvas 的图片压缩工具，上传前限制最大 1920×1920、JPEG quality 0.85。
- `CreateMemory` 与 `Memories` 的文件选择器接入压缩，减少 base64/Supabase 传输体积和 localStorage 占用。
- 修复 `src/api/memories.ts` 中 `updateMemoryMedia`：删除旧 media 重新插入时，同步写入 `note`、`likes`、`is_cover`，解决便签/点赞/封面刷新丢失。
- `Memories` 与 `MonthAlbum` 的日期卡片：没有便签时不再显示“点击照片添加便签…”占位文字；已有便签继续用中文引号 “” 包裹展示。
- `tsc -b` 与 `vite build --emptyOutDir=false` 均通过。
- 本地 git push 因当前 shell 凭证问题多次失败，需用户手动 push。

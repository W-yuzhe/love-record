# 恋爱记录网站 · 使用与配置清单

## 本次改动说明

1. **顶部导航扩展**：改为 **Home / Experience / Stars / Trip**，字体统一为与 `days of us` 一致的 Playfair Display 衬线斜体。
2. **首页重新设计**：中间显示 `xxx days of us`，底部横向滚动显示从相恋日到当前每个月份的卡片。
3. **月份卡片**：点击任意月份卡片，进入该月份的回忆列表。
4. **创建回忆录修复**：新增的回忆会保存到 Supabase（已配置时）或浏览器本地（LocalStorage）。
5. **支持上传视频**：创建回忆录和 Trip 足迹时均支持上传图片和视频。
6. **星空页改造**：极简星星，点击后随机跳转某条回忆或显示预设便签。
7. **Trip 足迹地图**：改为静态 SVG 中国地图（无需腾讯地图 Key），支持滚轮缩放、拖拽平移，点击城市高亮并弹出表单，保存后城市标记变为粉紫色。
8. **底部导航同步**：手机端底部保留 Home / Experience / Stars / Trip 四个入口。

---

## 当前可立即使用的功能（无需后端）

- 打开首页，看到相恋天数和月份时间线。
- 点击底部/顶部 **Experience** 进入回忆录。
- 点击 **+ Memory / 记一件小事** 创建新回忆。
- 上传图片、视频、文字、地点、天气、心情、标签。
- 创建后跳转到回忆录，新回忆会立即显示。
- 点击 **Stars** 进入星空页，点击星星随机查看回忆或便签。
- 点击 **Trip** 进入足迹地图，点击城市记录旅行回忆，保存后城市变粉紫色。
- 刷新页面后，已配置 Supabase 时数据从云端读取；未配置时本地创建的回忆仍然保留。

---

## 需要配置的清单

### 必填（让网站显示你们的信息）

在 `love-record-site/.env` 中填写：

```env
VITE_SITE_NAME=你们的站点名
VITE_PARTNER_A_NICKNAME=你的昵称
VITE_PARTNER_B_NICKNAME=对方昵称
VITE_LOVE_START_DATE=相恋起始日（如 2023-09-07）
```

> 不填会显示默认值：恋爱记录 / 小A / 小B / 2025-09-10。

### 功能相关配置

| 配置项 | 作用 | 当前状态 |
|--------|------|---------|
| Supabase URL + Anon Key | 数据保存到云端，换设备/清除浏览器数据不丢失 | **已配置，数据已启用云端存储** |
| 部署到 Vercel / Cloudflare / 自有服务器 | 让网站可以通过公网域名访问 | **功能调试完后再部署即可** |

> **Trip 足迹地图已改为静态 SVG 中国地图，不再需要腾讯地图 Key。**

---

## 4.2 / 4.3 / 4.4 是做什么的？

`DEPLOY.md` 的第四节是「部署到线上」，三个小节分别对应三种上线方式：

### 4.2 部署到 Vercel

- **作用**：把网站免费部署到 Vercel 平台，分配一个 `.vercel.app` 域名。
- **适合谁**：想快速上线、不想买服务器、熟悉 GitHub 的用户。
- **需要做什么**：把代码推送到 GitHub → 在 Vercel 导入仓库 → 填环境变量 → 一键部署。

### 4.3 部署到 Cloudflare Pages

- **作用**：把网站免费部署到 Cloudflare Pages，分配一个 `.pages.dev` 域名。
- **适合谁**：想使用 Cloudflare CDN、已有 Cloudflare 账号的用户。
- **需要做什么**：在 Cloudflare 创建项目 → 连接 GitHub → 填构建命令和环境变量 → 部署。

### 4.4 自有服务器 / CDN

- **作用**：把打包好的 `dist/` 文件夹上传到自己的服务器或对象存储。
- **适合谁**：有腾讯云 COS / 阿里云 OSS / 自有 VPS / 域名的用户。
- **需要做什么**：执行 `npm run build` → 上传 `dist/` 到服务器或对象存储 → 配置静态网站托管 → 绑定域名。

> **共同点**：三种方式都是把前端静态资源放到公网；数据持久化由 Supabase 负责，与部署方式无关。

---

## 常见问题：部署相关

**Q1：Supabase、部署，这两个能不能等功能调试完后再配置？**

> **可以。**
> - **Supabase**：你已经配好了，数据已经在云端；如果未配置，数据会暂存在浏览器本地。
> - **部署上线**：完全可以等功能全部调通、页面效果满意后再部署。本地 `npm run dev` 足够调试。
> - **腾讯地图 Key**：Trip 已改为静态 SVG 地图，不再需要配置。

**Q2：这三个是不是只要部署一个，就能实现多设备访问、持久上线、把链接分享给朋友、确保数据不会随浏览器清理而丢失？**

> **是的，部署一个就够了。**
> - **多设备访问 / 分享链接 / 持久上线**：只要部署到 Vercel、Cloudflare Pages 或自有服务器中的**任意一个**，网站就会有公网链接，手机、电脑、朋友都能打开。
> - **数据不随浏览器清理丢失**：这一点靠 **Supabase** 实现，和部署方式无关。你已经配置了 Supabase，所以即使不部署，数据也已经在云端；部署只是把前端放到公网，让大家能访问到这些云端数据。
> - **总结**：上线只需要选一种部署方式；数据持久化已经由 Supabase 负责。

---

## 使用步骤

### 1. 本地启动

在 VS Code 终端中：

```bash
cd C:/Users/王玉喆/WorkBuddy/2026-09-05-14-39-39/love-record-site
npm run dev
```

浏览器打开显示的地址（例如 `http://localhost:5174`）。

### 2. 修改基础信息

打开 `.env`，把默认信息改成你们的真实信息：

```env
VITE_SITE_NAME=我们的恋爱记录
VITE_PARTNER_A_NICKNAME=小逸
VITE_PARTNER_B_NICKNAME=小院
VITE_LOVE_START_DATE=2023-09-07
```

改完后按 `Ctrl + C` 停止 dev server，再 `npm run dev` 重新启动。

### 3. 创建第一条回忆

- 点击 **+ Memory** 或 **记一件小事**。
- 填写事件名称、日期、描述。
- 选择天气、心情、标签。
- 上传照片或视频。
- 点击 **保存记忆**。
- 保存成功后自动跳转到回忆录。

### 4. 查看月份时间线

回到首页，向下滑动到底部，可以看到从 `2023-09` 到当前月份的卡片。点击某个月份，进入该月的回忆列表。

### 5. 星空随机回忆

点击顶部 **Stars**，进入星空页，点击任意一颗星星：
- 50% 概率随机打开某条回忆详情。
- 50% 概率弹出一句星光便签。

### 6. Trip 足迹地图

点击顶部/底部 **Trip**：
- 地图为静态 SVG 中国地图，已预置 34 个主要城市。
- 使用鼠标滚轮或双指缩放地图，拖拽移动地图；右下角按钮可放大/缩小/重置视图。
- 点击地图上的城市圆点，城市高亮并弹出表单。
- 在表单中填写旅行回忆、上传照片/视频。
- 点击 **保存并点亮**，该城市标记变为粉紫色（`#D68FB8`）并带柔光。
- 页面下方会列出所有已点亮的城市，点击可再次编辑。

---

## 如何替换字体？

当前导航栏和 `days of us` 使用的是 **Playfair Display**（Google Fonts 提供的衬线体）。

### 去找新字体

1. 打开 [Google Fonts](https://fonts.google.com/)。
2. 在搜索框输入你想要的风格，例如：
   - 优雅衬线：`Cinzel`、`Cormorant Garamond`、`Libre Baskerville`
   - 手写感：`Dancing Script`、`Great Vibes`、`Parisienne`
   - 现代简约：`Montserrat`、`Lato`、`Nunito`
3. 点击喜欢的字体 → **「Get font」** → 复制页面提供的 `<link>` 代码。

### 让我马上替换

把你看中的字体名称告诉我（例如："我想换成 Dancing Script"），我会：
1. 修改 `index.html` 引入新字体。
2. 修改 `tailwind.config.ts` 的 `fontFamily`。
3. 重新构建项目。

**示例**：如果你想把导航和 `days of us` 换成更有手写感的字体，可以跟我说：「换成 Dancing Script」。

---

## 数据说明

| 模式 | 数据存储位置 | 特点 |
|------|------------|------|
| 云端模式 | Supabase 数据库 | 数据持久化，可多设备访问；需要 Supabase 配置正确且匿名登录成功 |
| 本地模式（回退） | 浏览器 LocalStorage | 当 Supabase 未配置、匿名登录失败或伴侣关系未初始化时自动使用；清除浏览器数据会丢失 |

当前优先尝试 Supabase 云端存储。如果认证失败，会自动回退到本地模式，确保你仍可继续创建回忆和点亮城市。

---

## 常见问题

**Q1：创建回忆后刷新页面，数据还在吗？**

> 在本地模式下，只要不清除浏览器缓存/LocalStorage，数据就会保留。

**Q2：换了电脑/手机还能看到这些回忆吗？**

> 本地模式不行。需要配置 Supabase 并部署到线上（4.2/4.3/4.4 任选一种），数据才会保存在云端。

**Q3：视频上传后只有我能看吗？**

> 本地模式下，视频以浏览器本地 URL 形式存在，只能在你当前这台设备的当前浏览器中播放。上线后会通过 Supabase Storage 存储，可多端访问。

**Q4：为什么首页月份卡片很多是空白的？**

> 月份卡片会自动生成从相恋日到当前的所有月份。如果某个月还没有创建回忆，卡片会显示「No memory yet」。创建该月的回忆后，卡片会自动显示封面。

**Q5：我什么时候需要上线部署？**

> 当你想：
> - 用手机/其他设备访问
> - 把链接分享给朋友
> - 确保数据不会随浏览器清理而丢失
> 
> 时再考虑部署。

**Q6：点击「保存记忆」后提示「invalid input syntax for type uuid: "local-couple"」怎么办？**

> 这说明 Supabase 已配置，但匿名登录没有成功，导致系统把 `local-couple` 当成了 UUID 往数据库里写。
>
> 解决步骤：
> 1. 打开 Supabase 控制台 → **Authentication → Providers**。
> 2. 找到 **Anonymous**（匿名登录），点击启用。
> 3. 保存后，刷新你的网页（`npm run dev` 保持运行，按 F5 刷新浏览器）。
> 4. 重新创建回忆，此时应该能正常写入 Supabase。
>
> 如果暂时不想开 Anonymous，创建的回忆也会自动保存到浏览器本地，不会丢失。

**Q7：Trip 足迹地图点击城市没有反应？**

> 排查顺序：
> 1. **是否配置了腾讯地图 Key？** 如果没有，Trip 页面顶部会显示黄色提示条。按上方「腾讯地图 Key 配置步骤」申请并填入 `.env`。
> 2. **Key 是否正确？** 确认 Key 是 Web 端（JavaScript API GL）类型，不是 WebService 或其他类型。
> 3. **Referer 白名单是否加了当前地址？** 本地开发一般是 `localhost:5173` 或实际端口（看终端输出）。
> 4. **白名单修改后是否等了几分钟？** 腾讯地图 Key 配置有缓存，修改白名单后可能需要 3-5 分钟生效。
> 5. **地图是否显示但标记点不明显？** 城市标记是白色小圆点，可以滚动放大后点击。
>
> 如果以上都确认无误仍无反应，按 F12 打开浏览器控制台，把报错截图发给我。

**Q8：Playfair Display 字体显示不出来，或加载很慢？**

> 当前字体从 Google Fonts 加载。国内网络访问 Google Fonts 可能较慢。
>
> 快速解决：
> - 告诉我你想换成什么字体，我可以把字体改为国内 CDN（如 360 字体镜像）或系统字体。
> - 或者直接把 `index.html` 里的 Google Fonts `<link>` 换成你本地已下载的字体文件。
>
> 如果你只是确认字体名称：导航栏和 `days of us` 现在用的就是 **Playfair Display**。

---

## 下一步可选操作

| 操作 | 优先级 | 说明 |
|------|--------|------|
| 配置腾讯地图 Key | **高** | Trip 足迹地图需要 |
| 替换首页背景图 | 低 | 把 `public/hero-bg.jpg` 换成你们喜欢的图 |
| 替换星空背景图 | 低 | 把 `public/star-sky.jpg` 换成星空图 |
| 替换字体 | 低 | 告诉我字体名称即可替换 |
| 部署上线 | 按需 | 功能调通后，选 DEPLOY.md 4.2/4.3/4.4 中的一种 |
